version: 2

sources:
  - name: haravan
    schema: raw_haravan  # Target schema created by dlt
    tables:
      - name: orders
        meta:
          dagster:
            asset_key: ["haravan", "orders"]
